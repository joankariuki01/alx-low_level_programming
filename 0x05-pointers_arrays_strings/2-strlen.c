#include "main.h"
/**
 * _strlen- main function
 *
 * @s: character to get string length
 *
 * Return: return length
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
